function find(array, value) {
	console.log(array.indexOf(value));
}

let products = [ "iPhone XS", "iPhone 12", "iPhone XR", "Iphone XS MAX", "iPhone 12 PRO MAX" ];
find(products, "Iphone XS MAX");
find(products, "iPhone XS");